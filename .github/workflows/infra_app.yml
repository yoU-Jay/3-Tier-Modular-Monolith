# name: CI/CD Pipeline

# on:
#   push:
#     branches:
#       - dev

# env:
#   AWS_REGION: us-east-1
#   ECR_REPO: my-ecr-repo
#   ECS_CLUSTER: my-ecs-cluster
#   ECS_SERVICE: my-ecs-service

# jobs:
#   detect-changes:
#     name: Detect Code Changes
#     runs-on: ubuntu-latest
#     outputs:
#       infra_changed: ${{ steps.filter.outputs.infra }}
#       app_changed: ${{ steps.filter.outputs.app }}
#     steps:
#       - uses: actions/checkout@v4

#       - uses: dorny/paths-filter@v3
#         id: filter
#         with:
#           filters: |
#             infra:
#               - 'infra/**'
#             app:
#               - 'app/**'

#   infra-jobs:
#     name: Deploy Infra
#     runs-on: ubuntu-latest
#     needs: detect-changes
#     if: ${{ needs.detect-changes.outputs.infra_changed == 'true' }}
#     outputs:
#       infra_affects_app: ${{ steps.conditional.outputs.affects_app }}
#     steps:
#       - uses: actions/checkout@v4

#       # Configure AWS credentials (OIDC preferred)
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::196031534418:role/GithubActions_youjay
#           aws-region: ${{ env.AWS_REGION }}

#       # Setup Terraform
#       - uses: hashicorp/setup-terraform@v3

#       - name: Terraform Init
#         run: terraform init
#         working-directory: infra/

#       - name: Terraform Apply
#         run: terraform apply -auto-approve
#         working-directory: infra/

#       # Capture Terraform outputs
#       - name: Capture Infra Outputs
#         id: tfoutputs
#         run: |
#           echo "ecr_repo=$(terraform output -raw ecr_repo)" >> $GITHUB_OUTPUT
#           echo "ecs_cluster=$(terraform output -raw ecs_cluster)" >> $GITHUB_OUTPUT
#           echo "ecs_service=$(terraform output -raw ecs_service)" >> $GITHUB_OUTPUT
#         working-directory: infra/

#       # Update SSM parameters only if changed
#       - name: Update Parameter Store
#         run: |
#           for key in ecr_repo ecs_cluster ecs_service; do
#             NEW_VALUE=$(echo "${{ steps.tfoutputs.outputs[$key] }}")
#             OLD_VALUE=$(aws ssm get-parameter --name "/ci/$key" --query "Parameter.Value" --output text 2>/dev/null || echo "")
#             if [ "$NEW_VALUE" != "$OLD_VALUE" ]; then
#               echo "Updating /ci/$key → $NEW_VALUE"
#               aws ssm put-parameter --name "/ci/$key" --value "$NEW_VALUE" --type String --overwrite
#               CHANGED=true
#             else
#               echo "/ci/$key unchanged"
#             fi
#           done
#           echo "affects_app=${CHANGED:-false}" >> $GITHUB_OUTPUT
#         id: conditional

#   app-jobs:
#     name: Build or Use Existing Image & Deploy
#     runs-on: ubuntu-latest
#     needs: [detect-changes, infra-jobs]
#     if: |
#       needs.detect-changes.outputs.app_changed == 'true' ||
#       needs.infra-jobs.outputs.infra_affects_app == 'true'
#     steps:
#       - uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::123456789012:role/GitHubActionsRole
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Fetch Infra Details
#         run: |
#           export ECR_REPO=$(aws ssm get-parameter --name "/ci/ecr_repo" --query "Parameter.Value" --output text)
#           export ECS_CLUSTER=$(aws ssm get-parameter --name "/ci/ecs_cluster" --query "Parameter.Value" --output text)
#           export ECS_SERVICE=$(aws ssm get-parameter --name "/ci/ecs_service" --query "Parameter.Value" --output text)
#           echo "ECR_REPO=$ECR_REPO" >> $GITHUB_ENV
#           echo "ECS_CLUSTER=$ECS_CLUSTER" >> $GITHUB_ENV
#           echo "ECS_SERVICE=$ECS_SERVICE" >> $GITHUB_ENV

#       - name: Login to Amazon ECR
#         run: |
#           aws ecr get-login-password --region ${{ env.AWS_REGION }} | \
#           docker login --username AWS --password-stdin ${{ env.ECR_REPO }}

#       # Branch 1: If App Changed, build new image
#       - name: Build & Push New Image
#         if: ${{ needs.detect-changes.outputs.app_changed == 'true' }}
#         run: |
#           IMAGE_TAG=build-${{ github.sha }}
#           docker build -t ${{ env.ECR_REPO }}:$IMAGE_TAG ./app
#           docker push ${{ env.ECR_REPO }}:$IMAGE_TAG
#           echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV

#       # Branch 2: If only Infra changed, check for existing image
#       - name: Check for Existing Image in ECR
#         if: ${{ needs.detect-changes.outputs.app_changed != 'true' }}
#         run: |
#           IMAGE_TAG=$(aws ecr list-images --repository-name ${{ env.ECR_REPO }} --query 'imageIds[0].imageTag' --output text)
#           if [ "$IMAGE_TAG" == "None" ] || [ -z "$IMAGE_TAG" ]; then
#             echo "❌ No existing image found in ${{ env.ECR_REPO }}"
#             exit 1
#           else
#             echo "✅ Found existing image: $IMAGE_TAG"
#             echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
#           fi

#       # Common step: Deploy to ECS
#       - name: Deploy to ECS
#         run: |
#           aws ecs update-service \
#             --cluster ${{ env.ECS_CLUSTER }} \
#             --service ${{ env.ECS_SERVICE }} \
#             --force-new-deployment
